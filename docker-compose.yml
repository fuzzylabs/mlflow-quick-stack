# ============================================================================
# MLflow Quick Stack — Docker Compose
# ============================================================================
# Services:
#   traefik              – HTTPS reverse proxy (ports 80 → 443 redirect, 443 TLS)
#   postgres             – Backend metadata store + MLflow auth DB
#   minio                – S3-compatible artifact store
#   create-bucket        – One-shot init container to create the S3 buckets
#   mlflow               – Tracking server with GenAI & basic-auth enabled
#   localai              – OpenAI-compatible local model server
#   open-webui           – Chat UI frontend for LocalAI + Ollama
#   label-studio-postgres – PostgreSQL for Label Studio
#   label-studio         – Data labeling platform (annotation UI)
#   label-studio-ml      – LLM ML backend bridging Label Studio ↔ LocalAI
# ============================================================================

name: mlflow-quick-stack

services:
  # ── Traefik reverse proxy ──────────────────────────────────────────────────
  traefik:
    image: traefik:v3.6
    container_name: traefik
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./volumes/traefik/traefik.yml:/etc/traefik/traefik.yml:ro
      - ./volumes/traefik/dynamic/:/etc/traefik/dynamic/:ro
      - ./volumes/traefik/certs/:/etc/traefik/certs/:ro
      - ./volumes/traefik/acme/:/letsencrypt/
    networks:
      - mlflow-network
    healthcheck:
      test: ["CMD", "traefik", "healthcheck"]
      interval: 10s
      timeout: 3s
      retries: 5

  # ── PostgreSQL ─────────────────────────────────────────────────────────────
  postgres:
    image: postgres:18
    container_name: mlflow-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - ./volumes/postgres:/var/lib/postgresql
    networks:
      - mlflow-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 10

  # ── MinIO (S3-compatible object storage) ───────────────────────────────────
  minio:
    image: quay.io/minio/minio:latest
    container_name: mlflow-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    ports:
      - "${LOCALHOST_MINIO_API_PORT:-9000}:9000"
      - "${LOCALHOST_MINIO_CONSOLE_PORT:-9001}:9001"
    volumes:
      - ./volumes/minio:/data
    networks:
      - mlflow-network
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 3s
      retries: 10

  # ── Bucket initialisation (run once) ───────────────────────────────────────
  create-bucket:
    image: quay.io/minio/mc:latest
    container_name: mlflow-create-bucket
    restart: "no"
    depends_on:
      minio:
        condition: service_healthy
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      S3_BUCKET_MLFLOW: ${S3_BUCKET_MLFLOW}
      S3_BUCKET_QUANTIZED_MODELS: ${S3_BUCKET_QUANTIZED_MODELS}
      S3_BUCKET_FULL_PRECISION_MODELS: ${S3_BUCKET_FULL_PRECISION_MODELS}
      S3_BUCKET_DATASETS: ${S3_BUCKET_DATASETS}
      S3_BUCKET_LABELSTUDIO: ${S3_BUCKET_LABELSTUDIO}
    entrypoint: >
      /bin/sh -c "
        mc alias set myminio http://minio:9000 $${MINIO_ROOT_USER} $${MINIO_ROOT_PASSWORD} &&
        mc mb --ignore-existing myminio/$${S3_BUCKET_MLFLOW} &&
        mc mb --ignore-existing myminio/$${S3_BUCKET_QUANTIZED_MODELS} &&
        mc mb --ignore-existing myminio/$${S3_BUCKET_FULL_PRECISION_MODELS} &&
        mc mb --ignore-existing myminio/$${S3_BUCKET_DATASETS} &&
        mc mb --ignore-existing myminio/$${S3_BUCKET_LABELSTUDIO} &&
        echo 'Buckets ready: '$${S3_BUCKET_MLFLOW}', '$${S3_BUCKET_QUANTIZED_MODELS}', '$${S3_BUCKET_FULL_PRECISION_MODELS}', '$${S3_BUCKET_DATASETS}', '$${S3_BUCKET_LABELSTUDIO}
      "
    networks:
      - mlflow-network

  # ── MLflow Tracking Server (GenAI) ─────────────────────────────────────
  mlflow:
    build:
      context: .
      dockerfile: mlflow.Dockerfile
      args:
        MLFLOW_VERSION: ${MLFLOW_VERSION:-v3.10.0}
    image: mlflow-quick-stack:latest
    container_name: mlflow-server
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      create-bucket:
        condition: service_completed_successfully
    environment:
      # Backend store
      MLFLOW_BACKEND_STORE_URI: "postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}"
      # Artifact store (S3 / MinIO)
      MLFLOW_ARTIFACTS_DESTINATION: "s3://${S3_BUCKET_MLFLOW}"
      MLFLOW_S3_ENDPOINT_URL: "http://minio:9000"
      MLFLOW_S3_IGNORE_TLS: "true"
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
      AWS_DEFAULT_REGION: ${AWS_DEFAULT_REGION}
      # Ollama (local LLM on host)
      OLLAMA_HOST: ${OLLAMA_BASE_URL}
      # MLflow built-in auth (required for AI Gateway)
      MLFLOW_AUTH_CONFIG_PATH: /mlflow/basic_auth.ini
      MLFLOW_FLASK_SERVER_SECRET_KEY: ${MLFLOW_FLASK_SERVER_SECRET_KEY}
      MLFLOW_CRYPTO_KEK_PASSPHRASE: ${MLFLOW_CRYPTO_KEK_PASSPHRASE}
    volumes:
      - ./volumes/mlflow/basic_auth.ini:/mlflow/basic_auth.ini:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "${LOCALHOST_MLFLOW_PORT:-5000}:${MLFLOW_PORT}"
    command: >
      mlflow server
        --backend-store-uri "postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}"
        --artifacts-destination "s3://${S3_BUCKET_MLFLOW}"
        --serve-artifacts
        --host 0.0.0.0
        --port ${MLFLOW_PORT}
        --allowed-hosts "${MLFLOW_DOMAIN},localhost:*,127.0.0.1:*"
        --cors-allowed-origins "https://${MLFLOW_DOMAIN},http://localhost:*"
        --app-name basic-auth
        -w 1
    networks:
      - mlflow-network
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:${MLFLOW_PORT}/health')\""]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s

  # ── LocalAI (OpenAI-compatible model server) ─────────────────────────────
  localai:
    image: localai/localai:${LOCALAI_IMAGE_TAG:-latest-cpu}
    container_name: localai
    restart: unless-stopped
    environment:
      LOCALAI_API_KEY: ${LOCALAI_API_KEY}
      LOCALAI_THREADS: ${LOCALAI_THREADS:-4}
      LOCALAI_CONTEXT_SIZE: ${LOCALAI_CONTEXT_SIZE:-2048}
      # Idle watchdog — auto-unload models after timeout
      LOCALAI_WATCHDOG_IDLE: "true"
      LOCALAI_WATCHDOG_IDLE_TIMEOUT: ${LOCALAI_WATCHDOG_IDLE_TIMEOUT:-15m}
      LOCALAI_WATCHDOG_BUSY: "true"
      LOCALAI_WATCHDOG_BUSY_TIMEOUT: ${LOCALAI_WATCHDOG_BUSY_TIMEOUT:-5m}
      # Memory safety — only one model loaded at a time (LRU eviction)
      LOCALAI_MAX_ACTIVE_BACKENDS: ${LOCALAI_MAX_ACTIVE_BACKENDS:-1}
      LOCALAI_CORS: "true"
    volumes:
      - ./volumes/localai/models:/build/models
    ports:
      - "${LOCALHOST_LOCALAI_PORT:-8080}:8080"
    networks:
      - mlflow-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/readyz"]
      interval: 30s
      timeout: 30s
      retries: 10
      start_period: 60s
    # ── NVIDIA GPU support (uncomment to enable) ──
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: all
    #           capabilities: [gpu]

  # ── Open WebUI (Chat interface) ────────────────────────────────────────────
  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    container_name: open-webui
    restart: unless-stopped
    depends_on:
      localai:
        condition: service_healthy
    environment:
      # Connect to LocalAI as an OpenAI-compatible backend
      OPENAI_API_BASE_URL: "http://localai:8080/v1"
      OPENAI_API_KEY: ${LOCALAI_API_KEY}
      # Also connect to host Ollama (both backends available in the UI)
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL}
      # Auth & security
      WEBUI_SECRET_KEY: ${OPENWEBUI_SECRET_KEY}
      WEBUI_AUTH: "True"
      # Auto-create admin account on first launch (no manual signup needed)
      WEBUI_ADMIN_EMAIL: ${OPENWEBUI_ADMIN_EMAIL:-admin@local.dev}
      WEBUI_ADMIN_PASSWORD: ${OPENWEBUI_ADMIN_PASSWORD:-change-me}
      WEBUI_ADMIN_NAME: ${OPENWEBUI_ADMIN_NAME:-Admin}
      ENABLE_INITIAL_ADMIN_SIGNUP: "False"
      # Allow users to generate API keys
      ENABLE_API_KEYS: "True"
      USER_PERMISSIONS_FEATURES_API_KEYS: "True"
      # Offline mode — no HuggingFace downloads or version checks
      OFFLINE_MODE: "true"
    volumes:
      - ./volumes/open-webui:/app/backend/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "${LOCALHOST_OPENWEBUI_PORT:-3000}:8080"
    networks:
      - mlflow-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s

  # ── Label Studio PostgreSQL ──────────────────────────────────────────────
  label-studio-postgres:
    image: postgres:18
    container_name: label-studio-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${LABELSTUDIO_POSTGRES_USER}
      POSTGRES_PASSWORD: ${LABELSTUDIO_POSTGRES_PASSWORD}
      POSTGRES_DB: ${LABELSTUDIO_POSTGRES_DB}
    volumes:
      - ./volumes/label-studio/postgres:/var/lib/postgresql
    networks:
      - mlflow-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${LABELSTUDIO_POSTGRES_USER} -d ${LABELSTUDIO_POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 10

  # ── Label Studio (Data Labeling Platform) ──────────────────────────────────
  label-studio:
    image: heartexlabs/label-studio:latest
    platform: linux/amd64
    container_name: label-studio
    restart: unless-stopped
    depends_on:
      label-studio-postgres:
        condition: service_healthy
    environment:
      # Database
      DJANGO_DB: default
      POSTGRE_NAME: ${LABELSTUDIO_POSTGRES_DB}
      POSTGRE_USER: ${LABELSTUDIO_POSTGRES_USER}
      POSTGRE_PASSWORD: ${LABELSTUDIO_POSTGRES_PASSWORD}
      POSTGRE_HOST: label-studio-postgres
      POSTGRE_PORT: "5432"
      # Admin account (auto-created on first launch)
      LABEL_STUDIO_USERNAME: ${LABELSTUDIO_ADMIN_EMAIL}
      LABEL_STUDIO_PASSWORD: ${LABELSTUDIO_ADMIN_PASSWORD}
      LABEL_STUDIO_USER_TOKEN: ${LABELSTUDIO_USER_TOKEN}
      # Security
      LABEL_STUDIO_DISABLE_SIGNUP_WITHOUT_LINK: "true"
      # External URL (for correct links in emails, webhooks, etc.)
      LABEL_STUDIO_HOST: "https://${LABELSTUDIO_DOMAIN}"
      CSRF_TRUSTED_ORIGINS: "https://${LABELSTUDIO_DOMAIN}"
      # Local file serving (for imported files)
      LABEL_STUDIO_LOCAL_FILES_SERVING_ENABLED: "true"
      LABEL_STUDIO_LOCAL_FILES_DOCUMENT_ROOT: /label-studio/data/files
      # Increase ML backend timeout for slow CPU inference (LocalAI)
      ML_TIMEOUT_PREDICT: "300"
      ML_CONNECTION_TIMEOUT: "10"
    volumes:
      - ./volumes/label-studio/app:/label-studio/data
    ports:
      - "${LOCALHOST_LABELSTUDIO_PORT:-8085}:8080"
    networks:
      - mlflow-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health/"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 90s

  # ── Label Studio ML Backend (LLM Interactive) ──────────────────────────────
  label-studio-ml:
    image: heartexlabs/label-studio-ml-backend:llm-master
    platform: linux/amd64
    container_name: label-studio-ml
    restart: unless-stopped
    depends_on:
      localai:
        condition: service_healthy
      label-studio:
        condition: service_healthy
    environment:
      # OpenAI-compatible provider (LocalAI)
      OPENAI_PROVIDER: openai
      OPENAI_API_KEY: ${LOCALAI_API_KEY}
      OPENAI_BASE_URL: "http://localai:8080/v1"
      OPENAI_MODEL: ${LABELSTUDIO_ML_MODEL:-gemma-3-1b-it}
      # Prompt configuration
      USE_INTERNAL_PROMPT_TEMPLATE: "1"
      PROMPT_PREFIX: prompt
      PROMPT_TEMPLATE: "You are a helpful AI assistant. Given the following context, answer the question accurately and concisely.\n\n--- Context ---\n{text}\n\n--- Question ---\n{prompt}\n\n--- Answer ---"
      TEMPERATURE: "0.7"
      NUM_RESPONSES: "1"
      # Label Studio connection
      LABEL_STUDIO_URL: "http://label-studio:8080"
      LABEL_STUDIO_API_KEY: ${LABELSTUDIO_USER_TOKEN}
      # Logging
      LOG_LEVEL: INFO
    volumes:
      - ./volumes/label-studio/ml:/data
    ports:
      - "${LOCALHOST_LABELSTUDIO_ML_PORT:-9090}:9090"
    networks:
      - mlflow-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9090/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

networks:
  mlflow-network:
    name: mlflow-network
